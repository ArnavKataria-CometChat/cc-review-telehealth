# ---- Server ----
PORT=4000
NODE_ENV=development

# Comma-separated list of allowed CORS origins (frontends). Use * for any.
CORS_ORIGIN=*

# ---- Auth ----
# Secret used to sign session JWTs. MUST be overridden in production.
JWT_SECRET=dev-only-change-me
# Token lifetime, e.g. 15m, 12h, 7d
JWT_EXPIRES_IN=12h
# bcrypt cost factor for password hashing
BCRYPT_ROUNDS=10

# Password used for all seeded demo accounts (see README).
SEED_PASSWORD=Passw0rd!

# ---------------------------------------------------------------------------
# Phase B (CometChat) placeholders — NOT used in Phase A. Documented here so the
# integration seam is obvious. Do not commit real values; keep secrets in env.
# ---------------------------------------------------------------------------
# COMETCHAT_APP_ID=
# COMETCHAT_REGION=
# COMETCHAT_AUTH_KEY=
# COMETCHAT_REST_API_KEY=
