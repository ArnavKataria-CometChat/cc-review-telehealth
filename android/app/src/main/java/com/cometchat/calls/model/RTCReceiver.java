package com.cometchat.calls.model;

/** No-op stub for legacy CallManager bytecode references (v6 calls — workaround W3). */
public class RTCReceiver {
    public RTCReceiver(String uid, String name, String type) {
    }
}
